# Command help
- create a new project `django-admin startproject <name> .`, the point is too add the repertory in the directory where your are
- create a new app `python manage.py startapp <name>`
- activate the env `source venv/bin/activate`
- deactivate the env `deacivate`
- launch server `python manage.py runserver`